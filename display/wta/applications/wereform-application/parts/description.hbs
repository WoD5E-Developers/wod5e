<section class="tab {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
    <prose-mirror name="system.forms.{{form}}.description" data-document-u-u-i-d="{{actor.uuid}}" value="{{formDescription}}"
        toggled="true" compact="true">
        {{{enrichedDescription}}}
    </prose-mirror>
</section>
