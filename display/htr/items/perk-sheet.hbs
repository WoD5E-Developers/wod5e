<header class="item-header">
    <img class="profile-img" src="{{img}}" data-edit="img" title="{{name}}" alt="Item Image" />
    <div class="header-fields">
        <h1 class="charname"><input name="name" type="text" value="{{name}}" placeholder="{{localize 'WOD5E.Name'}}" /></h1>
        <div class="flexrow flex-center header-details">
            <!-- Select the edge this perk belongs to -->
            <label>
                <b>{{localize "WOD5E.HTR.EdgeType"}}</b>
            </label>
            <select name="system.edge" data-type="String">
                {{selectOptions edgeOptions selected=selectedEdge localize=true}}
            </select>
        </div>
    </div>
</header>
