{{#if actor.isOwner}}
    <button type="button" class="lock-btn">
        {{#if locked}}
            <i class="fa fa-lock"></i>
        {{else}}
            <i class="fa fa-lock-open"></i>
        {{/if}}
    </button>
{{/if}}