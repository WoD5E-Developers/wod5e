import { WoDItemModel } from '../base-item-model.js'

export class AuspiceItemModel extends WoDItemModel {
  static defineSchema() {
    const schema = super.defineSchema()

    return schema
  }
}
